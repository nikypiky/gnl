The strlcpy() and tes
rings respectively. They are design
ncluded in si

e. Also note that strlcpy() and strlcat() only operate on true ''C'' strings. This means that for strlcpy() src must b

NUL-terminated and for strlcat()
both src and dst must be NUL-
 terminatedhhk.
