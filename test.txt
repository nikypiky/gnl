The strlcpy() and tested
rings respectively. They are design
included in si
Also note that strlcpy() and strlcat() only operate on true ''C'' strings.
NUL-terminated and for strlcat()
both src and dst must be NUL-
terminated.